pipeline {
    agent any

    stages {
        stage('verify tooling') {
            steps {
                sh '''
                echo $BACKUP_ID
                echo $COMMIT_ID
                '''
            }
        }
    }
}
